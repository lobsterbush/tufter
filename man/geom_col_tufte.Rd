% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bars.R
\docType{data}
\name{geom_col_tufte}
\alias{geom_col_tufte}
\alias{geom_bar_tufte}
\alias{GeomColTufte}
\title{Bar charts with the gridlines erased through the bars}
\usage{
geom_col_tufte(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "stack",
  ...,
  sides = c("y", "x"),
  rule_colour = "white",
  rule_linewidth = 0.6,
  minor = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_bar_tufte(
  mapping = NULL,
  data = NULL,
  stat = "count",
  position = "stack",
  ...,
  sides = c("y", "x"),
  rule_colour = "white",
  rule_linewidth = 0.6,
  minor = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, na.rm, show.legend, inherit.aes, ...}{Standard
\code{ggplot2} layer arguments. See \code{\link[ggplot2]{layer}()}.}

\item{sides}{Which axis's breaks to erase through the bars. \code{"y"} (the
default) suits vertical bars; \code{"x"} suits horizontal ones.}

\item{rule_colour}{Colour of the erased rules. Defaults to \code{"white"},
which is correct on a white page; set it to your background colour
otherwise.}

\item{rule_linewidth}{Width of the erased rules. Defaults to \code{0.6}.}

\item{minor}{Logical. Erase minor breaks as well as major ones? Defaults to
\code{FALSE}.}
}
\value{
A \code{ggplot2} layer.
}
\description{
Tufte's bar chart redesign is the clearest case of erasing redundant
data-ink. The gridlines are needed, because readers have to recover values
from bar heights. But a gridline crossing a bar is drawn on top of ink that
already encodes the same information, so it is erased there instead of being
drawn over the bar. The result is a bar with white rules through it, which
reads as a ruler laid against the data.
}
\details{
Use with \code{\link{theme_tufte}()} and \code{grid = "none"}: this layer
draws the white rules, and the theme should not add grey ones underneath.
}
\examples{
library(ggplot2)
d <- data.frame(
  who = c("Reagan", "Bush", "Clinton", "Bush", "Obama"),
  value = c(3.5, 2.3, 3.9, 2.1, 2.5)
)
ggplot(d, aes(who, value)) +
  geom_col_tufte(fill = "grey70") +
  theme_tufte()
}
\keyword{datasets}
