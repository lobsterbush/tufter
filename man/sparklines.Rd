% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparkline.R
\name{sparklines}
\alias{sparklines}
\title{Many sparklines at once}
\usage{
sparklines(
  data,
  x,
  y,
  group,
  band = c(0.25, 0.75),
  band_fill = "grey90",
  extremes = TRUE,
  label = TRUE,
  accuracy = 0.1,
  colour = "grey15",
  linewidth = 0.3
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y, group}{Bare column names for position, value and series.}

\item{band, band_fill, colour, linewidth, accuracy}{As in
\code{\link{sparkline}()}.}

\item{extremes}{Logical. Mark each series' minimum and maximum?}

\item{label}{Logical. Print each series' final value at the right?}
}
\value{
A \code{ggplot} object.
}
\description{
Draws one sparkline per series, stacked, with the series name to the left
and the final value to the right. This is the form Tufte uses for tables of
indicators, where the reader wants shape and level together.
}
\details{
Each series keeps its own vertical scale, because a sparkline reports the
shape of one series rather than inviting comparison of levels across
series. If you do want levels compared, use \code{\link{facet_tufte}()},
which fixes the scales.
}
\examples{
set.seed(1)
d <- data.frame(
  t = rep(1:40, 4),
  v = c(cumsum(rnorm(40)), cumsum(rnorm(40)), cumsum(rnorm(40)),
        cumsum(rnorm(40))),
  series = rep(c("Wheat", "Maize", "Rice", "Barley"), each = 40)
)
sparklines(d, t, v, series)
}
