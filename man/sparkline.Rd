% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparkline.R
\name{sparkline}
\alias{sparkline}
\title{Sparklines}
\usage{
sparkline(
  values,
  index = seq_along(values),
  band = c(0.25, 0.75),
  band_fill = "grey90",
  extremes = TRUE,
  last_point = TRUE,
  label = TRUE,
  accuracy = 0.1,
  big.mark = ",",
  colour = "grey15",
  linewidth = 0.3,
  extreme_colours = c("#4a6b82", "#a1483c")
)
}
\arguments{
\item{values}{Numeric vector of values, in order.}

\item{index}{Optional numeric vector of positions. Defaults to
\code{seq_along(values)}.}

\item{band}{Either \code{NULL} for no band, or a length-2 numeric vector of
quantiles defining the normal range. Defaults to \code{c(0.25, 0.75)}.}

\item{band_fill}{Fill colour of the normal-range band.}

\item{extremes}{Logical. Mark the minimum and maximum with dots?}

\item{last_point}{Logical. Mark the final value with a dot?}

\item{label}{Logical. Print the final value at the right-hand end?}

\item{accuracy}{Rounding for the printed value, passed to
\code{\link[scales]{label_number}()}. Defaults to \code{0.1}; use
\code{1} for counts.}

\item{big.mark}{Thousands separator for the printed value. Defaults to a
comma, since the \pkg{scales} default is a space and reads oddly at
sparkline size.}

\item{colour}{Line colour. Defaults to a near-black grey.}

\item{linewidth}{Line width. Defaults to \code{0.3}, a hairline.}

\item{extreme_colours}{Length-2 vector of colours for the minimum and
maximum dots.}
}
\value{
A \code{ggplot} object with no axes, sized to be printed small.
}
\description{
A sparkline is a word-sized graphic: a time series drawn small enough to sit
inside a sentence, a table cell, or a margin, where the reader meets it in
the same glance as the text around it. Tufte introduced them in
\emph{Beautiful Evidence} as the extreme case of data-ink maximisation, with
resolution traded for context.
}
\details{
The conventional furniture is a grey band showing the normal range, dots at
the minimum and maximum, and the final value printed at the right-hand end.
Everything else, including both axes, is gone.
}
\examples{
set.seed(1)
sparkline(cumsum(rnorm(80)))
}
\seealso{
\code{\link{sparklines}()} for many series at once,
  \code{\link{sparkline_grob}()} to embed one in other graphics.
}
