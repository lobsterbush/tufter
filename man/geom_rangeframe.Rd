% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rangeframe.R
\docType{data}
\name{geom_rangeframe}
\alias{geom_rangeframe}
\alias{GeomRangeFrame}
\alias{geom_quartileframe}
\alias{GeomQuartileFrame}
\title{Range frames and quartile frames}
\usage{
geom_rangeframe(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  sides = "bl",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_quartileframe(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  ...,
  sides = "bl",
  gap = 0.01,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, stat, position, na.rm, show.legend, inherit.aes, ...}{Standard
\code{ggplot2} layer arguments. See \code{\link[ggplot2]{layer}()}.}

\item{sides}{Which axes to draw, as a string containing any of \code{"b"}
(bottom), \code{"l"} (left), \code{"t"} (top) and \code{"r"} (right).
Defaults to \code{"bl"}.}

\item{gap}{For \code{geom_quartileframe()}, the width of the break at each
quartile, in npc units of the panel. Defaults to \code{0.01}.}
}
\value{
A \code{ggplot2} layer.
}
\description{
A range frame draws an axis line between the smallest and largest observed
values. Tufte's quartile frame adds breaks at the quartiles, so the axis also
shows the five-number summary.
}
\details{
Use these with \code{\link{theme_tufte}()}, which has no axis lines by
default. With another theme, turn off its panel border and axis lines if
you want only the range frame.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  geom_rangeframe() +
  theme_tufte()

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  geom_quartileframe() +
  scale_x_continuous(breaks = quartile_breaks(mtcars$wt)) +
  scale_y_continuous(breaks = quartile_breaks(mtcars$mpg)) +
  theme_tufte()
}
\seealso{
\code{\link{quartile_breaks}()}, which puts the axis labels in the
  same places the quartile frame breaks.
}
\keyword{datasets}
